import { RemoveElementDirective } from './remove-element.directive';

describe('RemoveElementDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveElementDirective();
    expect(directive).toBeTruthy();
  });
});
