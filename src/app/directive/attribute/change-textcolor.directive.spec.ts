import { ChangeTextcolorDirective } from './change-textcolor.directive';

describe('ChangeTextcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeTextcolorDirective();
    expect(directive).toBeTruthy();
  });
});
